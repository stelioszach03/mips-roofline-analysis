# Έλεγχος υπερχείλισης για a * b
li $t0, 2147483647    # Μέγιστος θετικός ακέραιος
div $t0, $t0, $a      # t0 = MAX_INT / a
blt $b, $t0, no_overflow
    li $v0, 10        # Κωδικός εξόδου για υπερχείλιση
    syscall
no_overflow:
    mul $v0, $a, $b   # Ασφαλής πολλαπλασιασμός
